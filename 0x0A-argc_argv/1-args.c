#include <stdio.h>

/**
 * main - check the code
 * @argc: number of arguments
 * @argv: array of string of argumments
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
(void) argv;
printf("%d\n", argc - 1);
return (0);
}
